const nome = 'edu'; //*
const nome1 = "edu";//*
const nome2 = `edu`; // strings
const num = 10; //number
const num1 = 10.52; //number
let noAlun; // undefined -> nao aponta para nenhum local na memória
const letsobrenomAluno = null; //nulo ->nao aponta para nenhum local na memória
const boolean = false; // Boolean = true, false (lógico)

console.log(typeof nome, nome)

const a = [1,2];
const b = a;
console.log(a,b); 

b.push(3);
console.log(a,b); 
